services:
  - type: web
    name: library-nova
    env: php
    buildCommand: composer install --no-dev && php artisan migrate --force && php artisan optimize
    startCommand: php artisan serve --host=0.0.0.0 --port=10000
